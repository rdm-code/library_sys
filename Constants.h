#include <string>

using PathStr = std::string;

const PathStr ROOT_PATH = "./data";
const PathStr USERS_PATH = ROOT_PATH + "/users.json";
const PathStr BOOKS_PATH = ROOT_PATH + "/books.json";
const PathStr BORROW_RECORD_PATH = ROOT_PATH + "/borrowRecord.json";